<%= turbo_frame_tag dom_id(@category, :sidebar), class: "category-frame" do %>
  <div class="category-editing">
    <%= render "categories/form", category: @category, cancel_href: item_category_path(@category) %>
  </div>
<% end %>
